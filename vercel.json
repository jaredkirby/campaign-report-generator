{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install --force",
  "framework": "nextjs",
  "functions": {
      "api/**/*.py": {
          "runtime": "python3.11"
      }
  },
  "env": {
      "EMAIL_SMTP_SERVER": "smtp.office365.com",
      "EMAIL_SMTP_PORT": "587"
  }
}